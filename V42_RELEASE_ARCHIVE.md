# V42 Release Archive

Release archive tooling records the release package and verifier surfaces. The archive is documentation/QA metadata; application data continues to use the SQL migrations under `migrations/`.
